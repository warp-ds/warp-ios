import SwiftUI

extension Warp {
    /// Represents the different styles a HelpText can have.
    enum HelpTextStyle: String, CaseIterable {
        case `default`
        case disabled
        case error
    }
}

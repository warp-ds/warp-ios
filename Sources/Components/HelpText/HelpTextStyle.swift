import SwiftUI

extension Warp {
    /// Represents the different styles a HelpText can have.
    public enum HelpTextStyle: String, CaseIterable {
        case `default`
        case disabled
        case error
    }
}

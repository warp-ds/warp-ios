extension Warp {
    /// Represents the different states a radio button can be in.
    public enum RadioButtonState: String, CaseIterable {
        case `default`
        case error
        case disabled
    }
}

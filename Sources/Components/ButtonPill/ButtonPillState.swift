import SwiftUI

extension Warp {
    /// Represents the interaction state of the `ButtonPill`.
    enum ButtonPillState {
        /// The default state when the button is inactive.
        case `default`
        /// The active state when the button is pressed or interacted with.
        case active
    }
}

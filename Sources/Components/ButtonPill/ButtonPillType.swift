import SwiftUI

extension Warp {
    /// Defines the type of `ButtonPill`, determining its appearance and behavior.
    public enum ButtonPillType {
        /// A pill with an overlay appearance.
        case overlay
        /// A pill with a flat appearance.
        case flat
    }
}
